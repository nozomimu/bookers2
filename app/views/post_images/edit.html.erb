      <div class='body'>
  <div class='container'>
        <% if @post_imagef.errors.any? %>
          <div id="error_explanation">
            <h2><%= @post_imagef.count %>件のエラー</h2>
            <ul>
            <% @post_image.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>
         <div class='row'>
            <h1>Editing Book</h1>
              <div class='col-xs-3'>
            </div>
              <div class='col-xs-9'>
     <%= form_for(@post_imagef) do |f| %>
  <div class="field row">
    <label for="book_title">Title</label>
    <%= f.text_field :title,class:"col-xs-12" %>

  <div class="field row">
    <label for="book_opinion">Opinion</label>
    <%= f.text_area :body,class:"col-xs-12" %>
  </div>

 <div class="actions row">
  <%= f.submit "Update Book", class:"btn btn-primary col-xs-12"%>
  </div>
  <% end %>

</form>
		<%= link_to 'show',post_image_path(@post_imagef.id) %> |
 <%=link_to "Back", books_path %>
</div></div></div>

 